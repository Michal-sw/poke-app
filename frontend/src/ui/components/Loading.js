import styled from "styled-components";

const Loading = () => {
  return (
    <LoadingText>
      Loading...
    </LoadingText>
  )
};

const LoadingText = styled.p`
  font-size: 3em;
`;
export default Loading;